{
  "name": "protocol-grist-adapter",
  "version": "1.0.0",
  "main": "src/server.ts",
  "repository": "https://github.com/Servant-Cities/protocol-grist-adapter",
  "license": "MIT",
  "scripts": {
    "build": "tsc --skipLibCheck --noEmit",
    "dev": "ts-node-dev --respawn --transpile-only --quiet --env-file=.env src/server.ts"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.2",
    "fastify": "^5.2.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.7.3"
  }
}
